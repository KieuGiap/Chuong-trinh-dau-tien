#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int thanhtien()
int  dongia,n, soluong;
	int n=4;
	for(i=1;i<4;i++)
	{
		printf(" don gia cua san pham thu %d la %d ",n,donggia);
		scanf("%d",&dongia);
		printf("so luong cua san pham thu %d la %d",n,soluong);
		scanf("%d",&soluong);
	}


int main(int argc, char *argv[]) {
	return 0;
}
